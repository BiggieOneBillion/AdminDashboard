/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "loremflickr.com",
      "picsum.photos",
      "prestige-calendar-dev.s3.us-east-1.amazonaws.com",
    ],
  },
};

export default nextConfig;
